import { type NextPage } from "next";
import Head from "next/head";
import DarkMode from "./DarkMode";
import { api } from "~/utils/api";
import { BrowserRouter as Router, Routes, Route, Link } from "react-router-dom";

import LandingPage from "./LandingPage";
import SearchForm from "./SearchForm";

const Home: NextPage = () => {
  const hello = api.example.hello.useQuery({ text: "from tRPC" });

  return (
    <Router>
      <div>
        <Head>
          <title>Plan - Prep - EAT!</title>
          <meta name="description" content="Generated by create-t3-app" />
          <link rel="icon" href="/favicon.ico" />
        </Head>

        <main className="// for DARK MODE flex h-screen flex-col items-center from-[#342944] to-[#0d0e12] text-black dark:bg-gradient-to-b  dark:text-white">
          <DarkMode />

          <div className="container flex flex-col items-center justify-center gap-12 px-4 py-6 ">
            <h1 className="text-5xl font-extrabold tracking-tight dark:text-white sm:text-[5rem]">
              Plan <span className="text-[hsl(280,100%,70%)]">PREP</span> Eat
            </h1>
          </div>

          <ul className="flex flex-row gap-10">
            <li>
              <Link className="hover:text-purple-600 hover:decoration-solid" to="/">HOME</Link>
            </li>
            <li>
              <Link className="hover:text-purple-600 hover:decoration-solid" to="/search">SEARCH</Link>
            </li>
          </ul>

          <Routes>
            <Route path="/search" element={<SearchForm />} />

            <Route path="/" element={<LandingPage />} />
          </Routes>
        </main>
      </div>
    </Router>
  );
};

export default Home;
